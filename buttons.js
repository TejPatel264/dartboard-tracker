const toPracticeMode = document.getElementById("practice")
const toGameMode = document.getElementById("game")
const toQuickStats = document.getElementById("quick-stats")

const bounceOut = document.getElementById("bounce-out");
const delLastThrow = document.getElementById("delete-last-throw");
const newLeg = document.getElementById("new-leg");
const backToHomeFromTracker = document.getElementById("home-page")
const toStats = document.getElementById("view-stats")

const sessionStats = document.getElementById("session-stats")
const allTimeStats = document.getElementById("all-time-stats")
const backToTracker = document.getElementById("back-to-tracker")
const backToHomeFromStats = document.getElementById("back-to-home")

const viewQuickStats = document.getElementById("btn-quick")
const viewScoringStats = document.getElementById("btn-scoring")

export const btn = {
    toPracticeMode,
    toGameMode,
    bounceOut,
    delLastThrow,
    newLeg,
    backToHomeFromTracker,
    toStats,
    toQuickStats,
    sessionStats,
    allTimeStats,
    backToTracker,
    backToHomeFromStats,
    viewQuickStats,
    viewScoringStats
}